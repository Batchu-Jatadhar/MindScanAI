__pycache__/
*.pyc
.venv/
venv/
.env
mindscan.db
# Model weights ARE committed: every file is under GitHub's 100MB limit
# (speech_encoder.pt is slimmed by training/slim_speech_checkpoint.py), so a
# fresh clone runs the real models instead of the mock pipeline.
data/
node_modules/
frontend/dist/
.DS_Store
.idea/
.vscode/
Screen Recording*.mov
/My Movie.mp4

# MediaPipe WASM runtime is copied out of node_modules by frontend postinstall.
frontend/public/mediapipe-wasm/

# Weight bundle for out-of-band transfer to teammates.
mindscan-weights.zip

# Machine-local Claude Code settings
.claude/

# Generated source bundle — regenerate with: python export_codebase.py
MindScanAI_codebase.txt
